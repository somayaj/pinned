# Local: copy to .env (never commit .env)
# Railway: set DATABASE_URL in the service Variables tab (Neon connection string).
PORT=3000
DATABASE_URL=postgresql://USER:PASSWORD@HOST/dbname?sslmode=require
